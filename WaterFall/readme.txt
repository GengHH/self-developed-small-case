本项目（WaterFall）流式布局，包含了三个子项目
  1、jsWaterFall ---采用原生的js+css(float) 布局，实现图片流式布局（等列宽）的样式；滚动时候动态懒加载图片功能；
  2、jqueryWaterFall ---采用原生的jquery+css(float) 布局，实现图片流式布局（等列宽）的样式；滚动时候动态懒加载图片功能；
  3、css3 （column-count属性）实现图片流式布局（等列宽）的样式；不能懒加载。

注：懒加载是采用了节流操作；